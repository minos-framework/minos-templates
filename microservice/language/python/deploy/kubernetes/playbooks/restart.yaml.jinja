---
- hosts: localhost
  connection: local
  tasks:
    - name: Restart {{ name }} Microservice
      shell: kubectl rollout restart --namespace={{ project_name }} deployment/microservice-{{ name }}
      register: results

    - debug: var=results.stdout_lines

    - debug: var=results.stderr_lines
