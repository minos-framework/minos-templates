apiVersion: v1
kind: Service
metadata:
  name: microservice-{{ name }}
  namespace: {{ project_name }}
spec:
  type: ClusterIP
  ports:
    - port: 8080
  selector:
   app.kubernetes.io/name: microservice-{{ name }}
