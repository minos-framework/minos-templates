apiVersion: v1
kind: Service
metadata:
  name: {{ project_name }}-redis
  namespace: {{ project_name }}
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
  selector:
   app.kubernetes.io/name: {{ project_name }}-redis