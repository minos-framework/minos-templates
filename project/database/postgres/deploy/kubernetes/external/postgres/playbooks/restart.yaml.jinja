---
- hosts: localhost
  connection: local
  tasks:
    - name: Restart Postgres
      shell: kubectl rollout restart --namespace={{ project_name }} deployment/postgres
      register: results

    - debug: var=results.stdout_lines

    - debug: var=results.stderr_lines
