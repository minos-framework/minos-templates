---
- hosts: localhost
  connection: local
  tasks:
    - name: Restart Zookeeper
      shell: kubectl rollout restart --namespace={{ project_name }} deployment/zookeeper
      register: results

    - debug: var=results.stdout_lines

    - debug: var=results.stderr_lines
