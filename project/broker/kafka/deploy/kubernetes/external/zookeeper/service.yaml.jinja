apiVersion: v1
kind: Service
metadata:
  name: {{ project_name }}-zookeeper
  namespace: {{ project_name }}
spec:
  type: ClusterIP
  ports:
    - port: 2181
      targetPort: 2181
  selector:
   app.kubernetes.io/name: {{ project_name }}-zookeeper