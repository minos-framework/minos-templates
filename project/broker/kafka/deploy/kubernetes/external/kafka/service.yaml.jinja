apiVersion: v1
kind: Service
metadata:
  name: {{ project_name }}-kafka
  namespace: {{ project_name }}
spec:
  type: ClusterIP
  ports:
    - port: 9092
      targetPort: 9092
  selector:
   app.kubernetes.io/name: {{ project_name }}-kafka