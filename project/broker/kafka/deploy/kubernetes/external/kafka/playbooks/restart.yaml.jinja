---
- hosts: localhost
  connection: local
  tasks:
    - name: Restart Kafka
      shell: kubectl rollout restart --namespace={{ project_name }} deployment/kafka
      register: results

    - debug: var=results.stdout_lines

    - debug: var=results.stderr_lines
