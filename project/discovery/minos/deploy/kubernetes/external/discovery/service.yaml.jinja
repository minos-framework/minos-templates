apiVersion: v1
kind: Service
metadata:
  name: discovery
  namespace: {{ project_name }}
spec:
  type: ClusterIP
  ports:
    - port: 5567
      targetPort: 5567
  selector:
   app.kubernetes.io/name: discovery