apiVersion: v1
kind: Service
metadata:
  name: {{ project_name }}-apigateway
  namespace: {{ project_name }}
spec:
  type: ClusterIP
  ports:
    - port: 5566
      targetPort: 5566
  selector:
   app.kubernetes.io/name: {{ project_name }}-apigateway